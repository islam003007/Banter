using Banter.SharedKernel;

namespace Banter.Domain.Messages;

public static class MessageErrors
{
    //public static Error AccessDenied(Guid conversationId)
    //    => Error.Forbidden("Messages.AccessDenied", $"You are not a member of the conversation with ID = {conversationId} to access it");
}
